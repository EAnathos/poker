[package]
name = "poker"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.36.2"
egui = "0.36.2"
rayon = "1"

[profile.release]
debug = 1        # line tables seulement — suffit pour samply
strip = "none"   # garde les symboles — obligatoire
